-set_meta_tags title:"レビュー数上位の書籍",description:"今月このサイトでレビュー数上位30位に入った書籍です。"
-if params[:latest]
  .has-text-centered
    %p
      #{Book.count()}件中,#{from = ((params[:page]||1).to_i-1)*15 + 1}～#{from+@books.count()-1}を表示
-else
  .has-text-centered
    %p 今月のレビュー数上位30の書籍
= render "partials/book_list"
-if params[:latest]
  .container
    = paginate @books